# CLAUDE.md

## 주석 정책

주석은 **코드와 문서를 읽어도 알 수 없는 것**만 남긴다. 코드가 이미 말하고 있는 내용을 다시 적지 않는다.

### 추가할 주석

- **히스토리**: 왜 이 선택을 했는지, 어떤 대안을 왜 버렸는지. 되돌리려는 다음 사람이 알아야 할 맥락.
- **버그 수정**: 우회 코드가 어떤 버그 때문에 존재하는지. 가능하면 버전이나 이슈 번호를 함께 적어 언제 걷어낼 수 있는지 알 수 있게 한다.
- **파악하기 힘든 코드**: 외부 제약(라이브러리 동작, 플랫폼 한계, 순서 의존성)이 코드 형태를 결정했고 그 이유가 코드에 드러나지 않는 경우.

```kotlin
// Compose Multiplatform 1.12+ no longer publishes iosX64 (Intel simulator) artifacts.
listOf(iosArm64(), iosSimulatorArm64())
```

### 제거할 주석

- 코드를 그대로 옮긴 설명 (`// 사용자 목록을 가져온다` 위의 `getUsers()`)
- 선언 이름을 반복하는 KDoc (`/** 플랫폼 이름 */` 위의 `platformName`)
- 파일이나 프로퍼티 접두사로 이미 드러나는 구획 라벨 (`# Android` 위의 `android.*`)
- 주석 처리된 죽은 코드 — 히스토리는 git이 관리한다
- `// TODO` 중 언제 누가 무엇을 할지 없는 것

### 판단 기준

주석을 지웠을 때 다음 사람이 **잘못된 수정을 할 위험**이 생기면 남긴다. 단지 읽는 시간이 조금 늘어날 뿐이면 지운다.
코드를 고칠 때 주석도 함께 고친다. 코드와 어긋난 주석은 없느니만 못하다.
